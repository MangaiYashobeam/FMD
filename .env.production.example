# ============================================================================
# FaceMyDealer - Production Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values
# ============================================================================

# ===========================================
# Domain & URLs
# ===========================================
DOMAIN=your-domain.com
FRONTEND_URL=https://your-domain.com
API_BASE_URL=https://your-domain.com
ACME_EMAIL=admin@your-domain.com

# ===========================================
# Database (PostgreSQL)
# ===========================================
POSTGRES_USER=facemydealer
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=facemydealer

# ===========================================
# Security Keys (GENERATE UNIQUE VALUES!)
# ===========================================
# Generate with: openssl rand -hex 32
JWT_SECRET=generate-a-64-char-hex-string-here
JWT_REFRESH_SECRET=generate-another-64-char-hex-string
ENCRYPTION_KEY=generate-32-byte-key-for-aes-256

# IIPC Super Admin Authentication
IIPC_SECRET=your-iipc-secret-key
SUPER_ADMIN_IPS=your-ip-here

# ===========================================
# External Services
# ===========================================
# OpenAI (for AI features)
OPENAI_API_KEY=sk-your-openai-key

# Resend (for email)
RESEND_API_KEY=re_your-resend-key

# Stripe (for payments)
STRIPE_SECRET_KEY=sk_live_your-stripe-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# AWS S3 (for file storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-bucket-name

# ===========================================
# Worker Configuration
# ===========================================
# Number of browser workers (each uses ~2GB RAM)
WORKER_REPLICAS=2

# Max browsers per worker
MAX_CONCURRENT_BROWSERS=3

# Max browsers in pool
MAX_BROWSERS_PER_POOL=10

# ===========================================
# Traefik Dashboard Auth (optional)
# ===========================================
# Generate with: htpasswd -nb admin yourpassword
TRAEFIK_AUTH=admin:$$apr1$$your-hashed-password
